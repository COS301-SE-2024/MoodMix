#Test for commiting the readme

<h1>Project Structure</h1>
<code>moodMix (project directory)
├── src
│   └── main
│       ├── java
│       │   └── com
│       │       └── five_dudes
│       │           └── moodMix
│       │               ├── controller
│       │               │   └── ApiController.java
│       │               └── MoodMixApplication.java
│       └── resources
│           └── application.properties (or .yaml)
├── target (auto-generated by Maven, contains compiled code and build artifacts)
├── pom.xml (Maven configuration file)
└── .mvn (Maven wrapper directory, if using Maven wrapper)
    ├── wrapper
    │   ├── maven-wrapper.jar
    │   └── maven-wrapper.properties
    └── wrapper.properties
</code>
